{
  "name": "template_vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "update": "tsc && vite build && butler push ./dist infinitenutshell/long-hexposure:html",
    "ghpages_kanvas2d": "cd .. && git clone https://github.com/knexator/kanvas2d.git && cd kanvas2d && npm i && npm run build:lib",
    "ghpages_build": "tsc && vite build --mode include_frame"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "glpk.js": "^4.0.2",
    "lil-gui": "^0.18.2",
    "twgl.js": "^5.5.3"
  }
}
